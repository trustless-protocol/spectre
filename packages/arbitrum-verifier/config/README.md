# Arbitrum attestor profile

The profile pins only the Arbitrum chain ID, router, commitment slot, canonical execution-header
fork, and `arbitrum_attestor_v1` artifact version. It contains no Ethereum client or BoLD settings.
