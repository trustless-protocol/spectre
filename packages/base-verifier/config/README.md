# Base attestor profile

The profile pins only the Base chain ID, router, commitment slot, canonical execution-header fork,
and `base_attestor_v1` artifact version. It contains no Ethereum client or dispute-game settings.
