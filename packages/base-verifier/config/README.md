# Base attestor profile

The profile pins the Base chain ID, router, commitment slot, canonical execution-header fork,
attestor Ed25519 public key, immutable attestation head, and `base_attestor_v2` artifact version. It contains no Ethereum client
or dispute-game settings.
