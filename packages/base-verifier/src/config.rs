//! Base runtime-profile types.

/// Base uses the shared OP Stack output-root format.
pub use op_stack_verifier::OutputRootFormat;
/// Immutable Base deployment profile carried in client state.
pub use op_stack_verifier::Profile as Config;
