# OP attestor profile

The profile pins the OP chain ID, router, commitment slot, canonical execution-header fork,
attestor Ed25519 public key, immutable attestation head, and `op_attestor_v2` artifact version. It contains no Ethereum client
or dispute-game settings.
