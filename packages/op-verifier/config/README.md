# OP attestor profile

The profile pins only the OP chain ID, router, commitment slot, canonical execution-header fork,
and `op_attestor_v1` artifact version. It contains no Ethereum client or dispute-game settings.
